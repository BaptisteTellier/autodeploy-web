// Placeholder — replaced by `scripts/fetch-vendor.sh` with the Tailwind Play
// CDN bundle. Run `make vendor` before `go build`.
console.warn("autodeploy-web: tailwind.js not vendored. Run `make vendor`.");
