# Copy this file to .env and edit as needed.
# docker compose reads .env automatically.

# Host port the UI is exposed on (default: 8080)
PORT=8080

# Maximum parallel ISO builds (default: 1 — raise carefully, disk-bound)
WORKER_CONCURRENCY=1
